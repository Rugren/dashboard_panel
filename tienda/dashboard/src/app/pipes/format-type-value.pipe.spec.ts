import { FormatTypeValuePipe } from './format-type-value.pipe';

describe('FormatTypeValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTypeValuePipe();
    expect(pipe).toBeTruthy();
  });
});
