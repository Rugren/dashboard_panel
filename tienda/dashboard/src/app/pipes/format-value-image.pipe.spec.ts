import { FormatValueImagePipe } from './format-value-image.pipe';

describe('FormatValueImagePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatValueImagePipe();
    expect(pipe).toBeTruthy();
  });
});
